project:
  seed: 146
  allow_dirty: false
paths:
  data: data
  models: models
  logs: logs
  output: output
  plots: plots
datasets:
  - config/datasets/mind-small.yaml
  - config/datasets/ebnerd-demo.yaml
retrieval:
  - config/retrieval/bm25.yaml
  - config/retrieval/bge.yaml
evaluation: config/evaluation.yaml
benchmark: config/benchmark.yaml
offline_impressions_limit: 10000
history_selection_limit: 10000
